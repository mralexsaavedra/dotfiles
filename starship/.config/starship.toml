# Starship Configuration - Engineering Edition
# Clean, informative, and focused on Web/Mobile dev stack

add_newline = true
command_timeout = 1000

# Order of modules
format = """
$directory\
$git_branch\
$git_status\
$fill\
$nodejs\
$bun\
$kotlin\
$java\
$swift\
$golang\
$rust\
$python\
$docker_context\
$package\
$cmd_duration\
$line_break\
$character\
"""

# 1. CHARACTER (Prompt)
[character]
success_symbol = "[➜](bold green)"
error_symbol = "[➜](bold red)"
vimcmd_symbol = "[❮](bold green)"

# 2. DIRECTORY
[directory]
style = "bold blue"
read_only = " 🔒"
truncation_length = 4
truncation_symbol = "…/"
home_symbol = "~"

# 3. GIT (The most important part)
[git_branch]
symbol = " "
style = "bold purple"
format = "[$symbol$branch]($style) "

[git_status]
style = "bold red"
conflicted = "🏳"
ahead = "⇡${count}"
behind = "⇣${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
up_to_date = ""
untracked = "?${count}"
stashed = "📦"
modified = "!${count}"
staged = "+${count}"
renamed = "»${count}"
deleted = "🗑${count}"
format = "[$all_status$ahead_behind]($style)"

# 4. UTILS
[cmd_duration]
min_time = 2000
format = "[$duration](bold yellow)"

[fill]
symbol = " "

# 5. LANGUAGES & STACKS (Web/Mobile focus)

[package]
symbol = "📦 "
style = "208" # Orange
format = "[$symbol$version]($style) "

[nodejs]
symbol = " "
style = "green"
not_capable_style = "red"
format = "[$symbol($version )]($style)"
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]
detect_files = ["package.json", ".nvmrc", ".node-version"]

[bun]
symbol = "🍞 "
style = "red"
format = "[$symbol($version )]($style)"

[kotlin]
symbol = "🅺 "
style = "blue"
format = "[$symbol($version )]($style)"

[java]
symbol = "☕ "
style = "red"
format = "[$symbol($version )]($style)"

[swift]
symbol = "🐦 "
style = "202" # Swift Orange
format = "[$symbol($version )]($style)"

[golang]
symbol = " "
style = "cyan"
format = "[$symbol($version )]($style)"

[rust]
symbol = "🦀 "
style = "208"
format = "[$symbol($version )]($style)"

[python]
symbol = "🐍 "
style = "yellow"
format = "[$symbol($version )]($style)"

[docker_context]
symbol = " "
style = "blue"
format = "[$symbol$context]($style) "
