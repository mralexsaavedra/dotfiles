# Codex consumer package

`~/.codex` consumes OpenCode canonical policy through symlinks.

Canonical references:

- `~/.config/opencode/AGENTS.md`
- `~/.config/opencode/rules/`
- `~/.config/opencode/skills/`

Keep files here only for true Codex-specific behavior.
