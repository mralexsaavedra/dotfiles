# Antigravity consumer package (within `ides/ai`)

Antigravity lives in the IDE/AI layer and consumes OpenCode canonical policy through symlinks under `~/.agent`.

Local override templates remain centralized in OpenCode (`opencode/.config/opencode/templates`).

Keep files here only when they are real Antigravity-specific settings/examples (no duplicated shared policy).
