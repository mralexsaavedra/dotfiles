# Gemini CLI consumer package

`~/.gemini/GEMINI.md` is a symlink to OpenCode canonical `AGENTS.md`.

Local override templates are centralized in OpenCode (`opencode/.config/opencode/templates`).

Keep files here only when they are real Gemini-specific settings/examples.
