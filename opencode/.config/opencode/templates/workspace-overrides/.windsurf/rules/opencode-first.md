# OpenCode-first workspace rule

This workspace aligns with OpenCode canonical AI policy.

Canonical references:

- `~/.config/opencode/AGENTS.md`
- `~/.config/opencode/rules/shared-ai-conventions.md`

Only add project-specific constraints here.
