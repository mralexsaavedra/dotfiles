# AGENTS local override (example)

Use this as a template for non-versioned local overrides.

Suggested usage (save locally as `AGENTS.local.md` if needed):

- machine-specific preferences
- environment-specific paths
- temporary workflow exceptions

Do not include secrets.
